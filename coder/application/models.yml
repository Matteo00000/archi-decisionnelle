models:
  - name: user_view_type
    description: "type of user view"
    columns:
      - name: user_view_type_id
        description: "Surrogate key for user_view_type Equipe hugo et matteo"
        data_tests:
          - not_null
          - unique
  - name: user
    description: "User ID"
    columns:
      - name: user_id
        description: "user id from the sources Equipe hugo et matteo"
        data_tests:
          - not_null
          - unique